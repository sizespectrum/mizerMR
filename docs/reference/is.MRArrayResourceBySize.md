# Test if an object is an MRArrayResourceBySize

Test if an object is an MRArrayResourceBySize

## Usage

``` r
is.MRArrayResourceBySize(x)
```

## Arguments

- x:

  An object to test.

## Value

`TRUE` if `x` is an `MRArrayResourceBySize` object, `FALSE` otherwise.
