# Test if an object is an MRArrayTimeByResourceBySize

Test if an object is an MRArrayTimeByResourceBySize

## Usage

``` r
is.MRArrayTimeByResourceBySize(x)
```

## Arguments

- x:

  An object to test.

## Value

`TRUE` if `x` is an `MRArrayTimeByResourceBySize` object, `FALSE`
otherwise.
